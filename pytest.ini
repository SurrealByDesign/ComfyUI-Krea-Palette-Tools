[pytest]
# See ComfyUI-Ideogram-Palette-and-Prompt-Tools/pytest.ini for the full explanation:
# repo dir name has hyphens, so pytest's package collection needs this to import
# the top-level __init__.py (ComfyUI's node-registration entry point) correctly.
consider_namespace_packages = true
